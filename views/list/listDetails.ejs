<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List Details</title>
</head>

<body>
    <%- include("../partials/navbar.ejs") %>
    <h1>
        <%= foundList.name %>
    </h1>

    <ul>
        <% if (foundList.tasks.length> 0) { %>
            <% foundList.tasks.forEach(task=> { %>
                <li>
                    <input type="checkbox" disabled <%=task.completed ? 'checked' : '' %> />
                    <%= task.content %>
                </li>
                <% }) %>
                    <% } else { %>
                        <li>No tasks in this list yet.</li>
                        <% } %>
                            <a href="/lists/edit/<%= foundList._id %>">Edit</a>
                            <form action="/lists/delete/<%= foundList._id %>?_method=DELETE" method="POST">
                                <button type="submit">Delete</button>
                            </form>
                           

    </ul>
    <a href="/auth/welcome">Home Page</a>
</body>

</html>